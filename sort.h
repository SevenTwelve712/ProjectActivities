#include <vector>
using namespace std;

vector<float> insertions_fvec_sort(vector<float> arr);
vector<float> shell_fvec_sort(vector<float> arr);
vector<float> selectionSort(std::vector<float>& arr);
vector<float> quickSort(std::vector<float>& arr);
